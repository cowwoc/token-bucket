/**
 * <b>Thread safety</b>: Assume that classes are not thread-safe, unless the Javadoc explicitly states
 * otherwise (e.g. {@link com.github.cowwoc.tokenbucket.Bucket}).
 */
package com.github.cowwoc.tokenbucket;